{
  "name": "serverless-image-optimizer",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda image optimization pipeline using S3 and Sharp.",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "lint": "node --check index.js",
    "test": "jest --runInBand",
    "package": "npm ci --omit=dev"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.992.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
